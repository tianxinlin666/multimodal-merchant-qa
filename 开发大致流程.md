# 多模态商帮问答系统 · 开发流程

## 技术栈

| 层级 | 选型 |
|------|------|
| 后端框架 | FastAPI (Python 3.11+) |
| 文本生成 | Qwen2.5-7B-Instruct，vLLM 本地部署 |
| 视觉理解 | Qwen2-VL-7B-Instruct，LMDeploy 本地部署 |
| Embedding | BGE-M3 (BAAI/bge-m3) |
| 向量库 | ChromaDB (嵌入式模式) |
| 稀疏检索 | BM25 (rank_bm25) |
| 重排序 | bge-reranker-v2-m3 (Cross-Encoder) |
| 前端 | React + TypeScript + Vite + Axios |

---

## 一、数据预处理

输入：约 100 本商帮历史文献 TXT（晋商/徽商/苏商/陕商/粤商/浙商/赣商），质量参差不齐（电子书 + OCR 扫描版）。

### 1.1 清洗

```
UTF-8 编码统一 → 去除连续空行 → 清除乱码/特殊符号 → 去除页码/页眉页脚 → 裁剪目录页/版权页
```

每本书输出干净文本 + metadata：

```json
{ "book_id": "book_001", "book_title": "晋商中国第一商帮", "source_type": "txt", "merchant_group_hint": "晋商" }
```

### 1.2 去重

- **文件级**：按文件名 + 文件大小标记疑似重复，人工确认
- **段落级**：MD5 精确去重，仅删除完全相同的段落。不用模糊匹配——不同表述即使语义相近也应保留

### 1.3 章节切分 → Chunk 分块

先按章节标题切分（正则匹配 `第X章` / `第X节`），再在每章内按 500 字切块。

每条 Chunk 的结构化记录：

```json
{
  "chunk_id": "book001_0023",
  "book_title": "晋商中国第一商帮",
  "chapter_title": "第一章 晋商的形成背景",
  "section_title": "一、山西地域与商路条件",
  "text": "...",
  "chunk_order": 23,
  "prev_chunk_id": "book001_0022",
  "next_chunk_id": "book001_0024"
}
```

`prev_chunk_id` / `next_chunk_id` 用于检索后的上下文补全。

---

## 二、索引构建

预处理完成后，将全部 Chunk 灌入两套索引：

| 索引 | 存储 | 内容 |
|------|------|------|
| **Dense** | ChromaDB | BGE-M3 Embedding (1024维) + text + metadata |
| **Sparse** | BM25 内存索引 | chunk_id + text + book_title + chapter_title |

---

## 三、Q&A 检索链路

### 纯文本问答

```
用户问题 → 混合检索(Dense top-20 + Sparse top-20) → RRF 融合(~30条)
→ bge-reranker-v2-m3 精排(top 6~8) → 相邻块补全 → Qwen2.5-7B 生成
```

### 图文联合问答

```
用户图片+问题 → Qwen2-VL 理解(caption/文字/关键词) → Query 扩充
→ 混合检索 → Reranker 精排 → 相邻块补全 → Qwen2.5-7B 生成
```

### 各环节要点

**混合检索**：Dense 负责语义匹配，Sparse 负责专有名词精确命中（商帮名、地名、制度名）。RRF 融合参数 k=60。

**重排序**：Cross-Encoder 将 (query, document) 拼接后直接输出相关性分数，比 Embedding 余弦相似度更精准。

**相邻块补全**：若命中 chunk_023，自动补回 chunk_022 和 chunk_024，前提是同一章节。防止因切块导致的语义断裂。

**Prompt 模板**：

```
基于以下从历史文献中检索到的信息回答用户的问题。如果信息不足以回答问题，请如实告知。

用户问题: {question}

相关资料:
- {context_1}
- {context_2}
...

回答:
```

---

## 四、模型本地部署

### 4.1 硬件要求与显存估算

| 场景 | Qwen2.5-7B | Qwen2-VL-7B | 合计 | 推荐 GPU |
|------|-----------|-------------|------|----------|
| FP16 双卡 | ~16 GB | ~18 GB | ~34 GB | 2× RTX 4090 或 A100 40G |
| INT4 量化单卡 | ~7 GB | ~9 GB | ~16 GB | 1× RTX 4090 (24G) |
| INT4 仅文本 | ~7 GB | — | ~7 GB | 1× RTX 4070 Ti (12G) |

> Qwen2-VL 比纯文本模型多一个 ViT 视觉编码器（约+1.5 GB）。

### 4.2 Qwen2.5-7B-Instruct（文本生成）

使用 vLLM 部署为 OpenAI 兼容 API，端口 8000：

```bash
pip install vllm

vllm serve Qwen/Qwen2.5-7B-Instruct \
    --host 0.0.0.0 \
    --port 8000 \
    --dtype bfloat16 \
    --max-model-len 8192 \
    --gpu-memory-utilization 0.9 \
    --api-key your-api-key-here
```

关键参数：
- `--dtype bfloat16` 需 Ampere+ 架构 GPU (RTX 3090/4090, A100)。旧卡用 `float16`
- `--gpu-memory-utilization 0.9` 为 KV Cache 预留 90% 显存
- vLLM 版本 ≥ 0.6.0

### 4.3 Qwen2-VL-7B-Instruct（视觉理解）

使用 LMDeploy 部署，封装为 FastAPI 服务，端口 8001：

```bash
pip install lmdeploy qwen_vl_utils
```

核心推理代码：

```python
from lmdeploy import pipeline

pipe = pipeline("Qwen/Qwen2-VL-7B-Instruct")

def analyze_image(image_base64: str, user_question: str) -> dict:
    messages = [{
        "role": "user",
        "content": [
            {"type": "text", "text": f"""分析这张图片，输出：
1. 图片内容描述  2. 图中可见文字  3. 关键词（逗号分隔）
用户问题：{user_question}"""},
            {"type": "image_url", "image_url": {"url": f"data:image/jpeg;base64,{image_base64}"}}
        ]
    }]
    response = pipe(messages)
    return {
        "caption": "...",
        "visible_text": "...",
        "keywords": ["..."]
    }
```

### 4.4 INT4 量化部署（显存不足时）

```bash
# GPTQ / AWQ 量化版直接加载
vllm serve Qwen/Qwen2.5-7B-Instruct-AWQ --quantization awq --port 8000
```

---

## 五、目录结构

```
multimodal-merchant-qa/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI 入口
│   │   ├── config.py            # 全局配置（Pydantic Settings）
│   │   ├── routers/
│   │   │   ├── qa.py            # POST /api/qa
│   │   │   └── upload.py        # POST /api/upload/image
│   │   ├── services/
│   │   │   ├── orchestrator.py  # 全流程编排
│   │   │   ├── retriever.py     # Dense + Sparse + RRF
│   │   │   ├── reranker.py      # Cross-Encoder 重排 + 上下文补全
│   │   │   ├── llm_client.py    # Qwen2.5-7B 客户端（OpenAI 兼容）
│   │   │   └── image_analyzer.py# Qwen2-VL 图片理解
│   │   └── utils/
│   │       ├── text_cleaner.py  # 清洗/去噪/元数据
│   │       └── chunking.py      # 章节切分 + Chunk 分块
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── App.tsx
│   │   └── api.ts
│   ├── package.json
│   └── vite.config.ts
└── data/
    ├── raw_txt/                 # 原始文献（gitignored）
    ├── cleaned/                 # 清洗后文本
    └── chroma_db/               # 向量库持久化
```

---

## 六、启动流程

```bash
# 1. 启动 Qwen2.5-7B（需 GPU，端口 8000）
vllm serve Qwen/Qwen2.5-7B-Instruct --port 8000 --api-key local-key

# 2. 启动 Qwen2-VL（需 GPU，端口 8001）
python -m app.services.image_analyzer

# 3. 启动后端 API（端口 8080）
cd backend && uvicorn app.main:app --reload --port 8080

# 4. 启动前端（端口 5173）
cd frontend && npm install && npm run dev
```
